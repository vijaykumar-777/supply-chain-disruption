---
phase: 01-data-nlp-foundation
plan: 04
type: execute
wave: 3
depends_on: ["01-02"]
files_modified: ["src/nlp/ner_extractor.py"]
autonomous: true
requirements: ["NLP-03", "NLP-04"]
must_haves:
  truths:
    - "System uses local spaCy to identify Named Entities (GPE, LOC, ORG) from unstructured text"
    - "System structures extracted nodes to prepare for Graph integration"
  artifacts:
    - path: "src/nlp/ner_extractor.py"
      provides: "Named Entity Extraction"
  key_links: []
---

<objective>
Implement Named Entity Recognition (NER) and entity linking.

Purpose: Extracts geographical points (countries, cities, ports) and organizations from news text to act as Nodes in the downstream Neo4j graph.
Output: Integrated `ner_extractor.py` parsing text into linked geographical or organizational entities.
</objective>

<execution_context>
@~/.gemini/antigravity/get-shit-done/workflows/execute-plan.md
@~/.gemini/antigravity/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/01-data-nlp-foundation/01-CONTEXT.md
@.planning/phases/01-data-nlp-foundation/01-02-SUMMARY.md
</context>

<tasks>
<task type="auto">
  <name>Task 1: Build NER Extractor Logic</name>
  <files>src/nlp/ner_extractor.py</files>
  <read_first>src/nlp/ner_extractor.py</read_first>
  <action>Create `src/nlp/ner_extractor.py` and a class `EntityExtractor`. Implement `extract_entities(self, text: str) -> dict`. Load the previously downloaded spaCy model `en_core_web_sm`. Process the raw string and yield dictionaries categorizing findings: return a list of locations (entities marked `GPE`, `LOC`) and organizations (entities marked `ORG`). Return these extracted strings as distinct arrays mapping to `{"locations": [...], "organizations": [...]}`.</action>
  <verify>python -c "import src.nlp.ner_extractor"</verify>
  <acceptance_criteria>
    - ner_extractor.py contains "class EntityExtractor"
    - Extracts "GPE", "LOC", and "ORG" entities from text using spaCy labels
  </acceptance_criteria>
</task>

<task type="auto">
  <name>Task 2: Geocoding Node resolution (Stub)</name>
  <files>src/nlp/ner_extractor.py</files>
  <read_first>src/nlp/ner_extractor.py</read_first>
  <action>Inside `EntityExtractor`, add a method `link_entities_to_nodes(self, extracted: dict) -> list`. For now, just format the returned strings to be capital-case standardized IDs representing preliminary Nodes (e.g. `New York` -> `NODE_NEW_YORK`). This acts as the connector stub for Phase 2 Graph generation mapping.</action>
  <verify>grep "link_entities_to_nodes" src/nlp/ner_extractor.py</verify>
  <acceptance_criteria>
    - ner_extractor.py contains "def link_entities_to_nodes"
    - Creates formatted node IDs from text
  </acceptance_criteria>
</task>
</tasks>

<verification>
- [ ] Returns valid structure dictionary for sample text.
- [ ] Nodes returned are reliably standardized strings.
</verification>

<success_criteria>
- All tasks completed
- SpaCy effectively links unstructured news strings into defined geographical nodes.
</success_criteria>

<output>
After completion, create `.planning/phases/01-data-nlp-foundation/01-04-SUMMARY.md`
</output>
