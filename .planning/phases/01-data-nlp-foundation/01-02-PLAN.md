---
phase: 01-data-nlp-foundation
plan: 02
type: execute
wave: 2
depends_on: ["01-01"]
files_modified: ["requirements.txt", "src/nlp/preprocessor.py", "scripts/download_models.py"]
autonomous: true
requirements: ["NLP-01"]
must_haves:
  truths:
    - "System tokenizes text, removes punctuation and stopwords"
    - "System processes text using local spaCy models (no API limits)"
  artifacts:
    - path: "src/nlp/preprocessor.py"
      provides: "Text cleaning logic"
    - path: "scripts/download_models.py"
      provides: "Downloads spaCy en_core_web_sm"
  key_links: []
---

<objective>
Implement the NLP preprocessing layer with spaCy/NLTK.

Purpose: Cleans the raw unstructured event data (news strings) into structured standard formats that can be classified and mapped.
Output: Helper scripts to download NLP models, and the `Preprocessor` logic inside `src/nlp`.
</objective>

<execution_context>
@~/.gemini/antigravity/get-shit-done/workflows/execute-plan.md
@~/.gemini/antigravity/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-data-nlp-foundation/01-CONTEXT.md
@.planning/phases/01-data-nlp-foundation/01-01-SUMMARY.md
</context>

<tasks>
<task type="auto">
  <name>Task 1: Add NLP dependencies and download script</name>
  <files>requirements.txt, scripts/download_models.py</files>
  <read_first>requirements.txt</read_first>
  <action>Append `spacy` to `requirements.txt`. Create `scripts/download_models.py` which uses `subprocess` to run `python -m spacy download en_core_web_sm`. Ensure `scripts/` directory is created. Make script executable with `chmod +x` equivalent standard processing logic or just document it as runnable.</action>
  <verify>python -c "import spacy"</verify>
  <acceptance_criteria>
    - requirements.txt contains "spacy"
    - scripts/download_models.py contains "python -m spacy download"
  </acceptance_criteria>
</task>

<task type="auto">
  <name>Task 2: Build Preprocessor Logic</name>
  <files>src/nlp/preprocessor.py</files>
  <read_first>src/nlp/preprocessor.py</read_first>
  <action>Create `preprocessor.py` with class `TextPreprocessor`. Implement `clean_text(self, text: str) -> str`. It should load the spaCy `en_core_web_sm` model (wrapped in a try-except to log if it's not downloaded), lower-case the text, tokenize, and remove string punctuations, spaces, and standard stop words. Return the clean string joined by spaces. Implement a dummy NLTK fallback if desired, or skip NLTK and stick exclusively to spaCy for simplicity.</action>
  <verify>python -c "import src.nlp.preprocessor"</verify>
  <acceptance_criteria>
    - preprocessor.py contains "class TextPreprocessor"
    - preprocessor.py imports "spacy" and loads "en_core_web_sm"
    - clean_text skips `token.is_stop` and `token.is_punct`
  </acceptance_criteria>
</task>
</tasks>

<verification>
- [ ] Dependencies have `spacy` added.
- [ ] `clean_text` correctly returns tokenized text without stopwords.
</verification>

<success_criteria>
- All tasks completed
- `src/nlp/" module defined.
- Preprocessor successfully uses local models.
</success_criteria>

<output>
After completion, create `.planning/phases/01-data-nlp-foundation/01-02-SUMMARY.md`
</output>
