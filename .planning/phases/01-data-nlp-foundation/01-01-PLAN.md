---
phase: 01-data-nlp-foundation
plan: 01
type: execute
wave: 1
depends_on: []
files_modified: ["requirements.txt", "src/config.py", "src/ingestion/gdelt_client.py", "src/ingestion/weather_client.py"]
autonomous: true
requirements: ["INGEST-01", "INGEST-02", "INGEST-03"]
must_haves:
  truths:
    - "System downloads recent GDELT event data in CSV/JSON format"
    - "System fetches weather data from OpenWeatherMap"
    - "Raw event data is stored locally in SQLite or basic JSON"
  artifacts:
    - path: "src/ingestion/gdelt_client.py"
      provides: "GDELT integration logic"
    - path: "src/ingestion/weather_client.py"
      provides: "OpenWeatherMap integration logic"
  key_links: []
---

<objective>
Setup repository structure and build the data ingestion pipeline (GDELT/Weather).

Purpose: Provides the raw unstructured data foundation for the supply chain monitoring system without incurring paid API costs.
Output: Initialized Python repository, GDELT fetching script, and OpenWeatherMap fetching script saving to local storage.
</objective>

<execution_context>
@~/.gemini/antigravity/get-shit-done/workflows/execute-plan.md
@~/.gemini/antigravity/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-data-nlp-foundation/01-CONTEXT.md
</context>

<tasks>
<task type="auto">
  <name>Task 1: Initialize project structure and dependencies</name>
  <files>requirements.txt, src/config.py</files>
  <read_first>.planning/phases/01-data-nlp-foundation/01-CONTEXT.md</read_first>
  <action>Create standard Python project structure (`src/`, `data/raw/`). Create `requirements.txt` containing `requests`, `python-dotenv`, and `pandas`. Create `src/config.py` using `python-dotenv` to load `OPENWEATHERMAP_API_KEY` from `.env`. Ensure `data/raw/` is added to `.gitignore`.</action>
  <verify>test -d src && test -f requirements.txt && test -f src/config.py</verify>
  <acceptance_criteria>
    - requirements.txt contains "requests", "python-dotenv", "pandas"
    - src/config.py imports "os", "dotenv" and loads OPENWEATHERMAP_API_KEY
    - .gitignore contains "data/raw/" and ".env"
  </acceptance_criteria>
</task>

<task type="auto">
  <name>Task 2: Build GDELT Ingestion Client</name>
  <files>src/ingestion/gdelt_client.py</files>
  <read_first>src/config.py</read_first>
  <action>Create `gdelt_client.py` with a class `GDELTClient`. Implement `fetch_latest_events(self, output_dir="data/raw/")` which hits the GDELT 2.0 CSV API (http://data.gdeltproject.org/gdeltv2/lastupdate.txt), parses the master URL, downloads the latest CSV export zip, unzips it, and saves the CSV to `data/raw/`. Handle HTTP exceptions.</action>
  <verify>python -c "import src.ingestion.gdelt_client"</verify>
  <acceptance_criteria>
    - gdelt_client.py contains "class GDELTClient"
    - gdelt_client.py uses the "requests" library to fetch "http://data.gdeltproject.org/gdeltv2/lastupdate.txt"
    - gdelt_client.py unzips the payload and saves to "data/raw/"
  </acceptance_criteria>
</task>

<task type="auto">
  <name>Task 3: Build OpenWeatherMap Ingestion Client</name>
  <files>src/ingestion/weather_client.py</files>
  <read_first>src/config.py</read_first>
  <action>Create `weather_client.py` with `WeatherClient`. Implement `fetch_weather(self, lat: float, lon: float, output_dir="data/raw/")` using `requests.get("https://api.openweathermap.org/data/2.5/weather", params={"lat": lat, "lon": lon, "appid": config.OPENWEATHERMAP_API_KEY})`. Save the raw JSON response to `data/raw/weather_lat_lon.json`.</action>
  <verify>python -c "import src.ingestion.weather_client"</verify>
  <acceptance_criteria>
    - weather_client.py contains "class WeatherClient"
    - weather_client.py uses "https://api.openweathermap.org/data/2.5/weather"
    - weather_client.py saves output to "data/raw/weather_"
  </acceptance_criteria>
</task>
</tasks>

<verification>
- [ ] Dependencies documented and installable.
- [ ] GDELT API endpoints correctly queried.
- [ ] OpenWeatherMap API uses the provided `appid` from config.
</verification>

<success_criteria>
- All tasks completed
- `src/ingestion/` module defined.
- `GDELTClient` and `WeatherClient` implemented correctly.
</success_criteria>

<output>
After completion, create `.planning/phases/01-data-nlp-foundation/01-01-SUMMARY.md`
</output>
