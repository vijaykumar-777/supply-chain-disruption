---
phase: 01-data-nlp-foundation
plan: 03
type: execute
wave: 3
depends_on: ["01-02"]
files_modified: ["requirements.txt", "src/nlp/classifier.py"]
autonomous: true
requirements: ["NLP-02"]
must_haves:
  truths:
    - "System classifies text into predefined supply chain event categories without hitting external APIs"
    - "System downloads the HuggingFace model once and runs entirely locally"
  artifacts:
    - path: "src/nlp/classifier.py"
      provides: "Event classification logic"
  key_links: []
---

<objective>
Develop the event classifier using local HuggingFace models.

Purpose: Identifies supply chain disruptions (e.g., strikes, disasters, port closures) by running open-source transformer models (zero-shot classification) locally. Ensures 100% free inferencing.
Output: Integrated `huggingface_hub` and `transformers` package dependencies. Created `EventClassifier` class.
</objective>

<execution_context>
@~/.gemini/antigravity/get-shit-done/workflows/execute-plan.md
@~/.gemini/antigravity/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-data-nlp-foundation/01-CONTEXT.md
</context>

<tasks>
<task type="auto">
  <name>Task 1: Add Transformers dependencies</name>
  <files>requirements.txt</files>
  <read_first>requirements.txt</read_first>
  <action>Add `transformers` and `torch` to `requirements.txt`. Make sure to avoid heavy versions if unnecessary (vanilla install of `transformers` and `torch` is fine for now on Mac).</action>
  <verify>grep transformers requirements.txt</verify>
  <acceptance_criteria>
    - requirements.txt contains "transformers" and "torch"
  </acceptance_criteria>
</task>

<task type="auto">
  <name>Task 2: Build Zero-Shot Classifier</name>
  <files>src/nlp/classifier.py</files>
  <read_first>src/nlp/classifier.py</read_first>
  <action>Create `src/nlp/classifier.py` with `EventClassifier`. Implement `classify_event(self, text: str) -> dict`. In the init, instantiate a pipeline using `transformers.pipeline("zero-shot-classification", model="facebook/bart-large-mnli")` (or `cross-encoder/nli-distilroberta-base` for speed, if preferred). Define default candidate labels like `["port strike", "natural disaster", "bankruptcy", "production halt", "extreme weather", "supply chain disruption"]`. Return the top label and its score in a formatted dictionary.</action>
  <verify>python -c "import src.nlp.classifier"</verify>
  <acceptance_criteria>
    - classifier.py contains "class EventClassifier"
    - classifier.py uses transformers "pipeline" initialized with "zero-shot-classification"
    - Defines supply chain specific labels
  </acceptance_criteria>
</task>
</tasks>

<verification>
- [ ] Dependencies updated for HuggingFace.
- [ ] Classification pipeline loads locally without requiring user interaction.
</verification>

<success_criteria>
- All tasks completed
- Zero-Shot classifier successfully processes arbitrary news text strings into predicted supply chain events.
</success_criteria>

<output>
After completion, create `.planning/phases/01-data-nlp-foundation/01-03-SUMMARY.md`
</output>
